apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: nginx-demo-app-1
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/h0perium/argocd-demo.git
    targetRevision: main
    path: k8s/nginx/overlays/product

  destination:
    server: https://kubernetes.default.svc
    namespace: product

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
